import { Base } from '../ui/base';
import { Roadmap } from '../ui/views/roadmap';

const RoadmapPage = () => {
  return (
    <Base withNav>
      <Roadmap />
    </Base>
  );
};

export default RoadmapPage;
