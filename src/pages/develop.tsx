import { Base } from '../ui/base';
import { Develop } from '../ui/views/develop';

const DevelopPage = () => {
  return (
    <Base withNav>
      <Develop />
    </Base>
  );
};

export default DevelopPage;
